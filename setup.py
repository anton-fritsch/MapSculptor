from distutils.core import setup

setup(name='MapSculptor',
      version='0.1',
      description='Map generation utilities for 2D tilemaps',
      author="Anton Fritsch",
      packages=['mapsculptor'],
      install_requires=['numpy'],
)
